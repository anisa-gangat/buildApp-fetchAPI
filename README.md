I used previous templates from lessons. I tried to change the background color but it wouldn't change to the chosen the color. I removed the dot before the 'body' tag and the background color appeared.

I was able to get a gradient background with a click using an event listener instead of a button click to make the page look tidy.
I used mdm web and chat gpt to trial out. I removed the button : <button onclick="changeGradient()">Change Gradient</button>....and instead used a mouse click anywhere on the page to trigger the gradient change. Now when clicking on the page, the background gradient changes based on two randomly generated colors. I kept the 'coffee' button.

I learned how absolute positioning was not user friendly so I decided to change the positioning.
I used Chatgpt because it gave me the best option for the page layout. I was struggling with positioning the items so I took this chart into consideration:

Method | Horizontal | Vertical | Responsive| Best For
Flex box | ✅ | ✅ | ✅ | Most layout centering
Grid | ✅ | ✅ | ✅ |Simple full-page centering
Text-align | ✅ | ❌ | ✅ |Inline/text-only elements
Absolute + Transform | ✅ | ✅ | ❌ |Small elements, modals

I tried using the functions taught in fetching the API. I was unsuccessful in attaining the response. :(/;)

notedByAnisa
